import test from 'node:test';import assert from 'node:assert/strict';import {controllerLaneState,controllerPosition} from '../src/experimental/controller-lane.js';
test('controller lane isolates channel/type/controller and sorts without mutation',()=>{const events=[{id:'late',start:2,type:'controlChange',parameter:11,channel:0},{id:'other',start:0,type:'controlChange',parameter:7,channel:0},{id:'early',start:1,type:'controlChange',parameter:11,channel:0},{id:'channel2',start:0,type:'controlChange',parameter:11,channel:1},{id:'bend',start:1,type:'pitchBend',parameter:0,channel:0}],original=structuredClone(events);assert.deepEqual(controllerLaneState({events},{}).events.map(e=>e.id),['early','late']);assert.deepEqual(controllerLaneState({events},{controllerChannel:1}).events.map(e=>e.id),['channel2']);assert.deepEqual(controllerLaneState({events},{controllerLane:'bend'}).events.map(e=>e.id),['bend']);assert.deepEqual(events,original);});
test('graph coordinates snap time, clamp region bounds and retain full pitch-bend range',()=>{assert.deepEqual(controllerPosition(73,80,{duration:4,max:127,step:.25}),{start:.5,value:64});assert.deepEqual(controllerPosition(-100,900,{duration:4,max:16383,step:0}),{start:0,value:0});assert.deepEqual(controllerPosition(9999,-90,{duration:4,max:16383,step:.25}),{start:4,value:16383});assert.equal(controllerPosition(73,80,{duration:4,max:127,step:0}).start,.5625);});
