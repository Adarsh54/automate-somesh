export function chooseProjectType(){return new Promise(resolve=>{
 const dialog=document.createElement('dialog');dialog.className='resume-workspace project-type-dialog';dialog.setAttribute('aria-labelledby','project-type-heading');dialog.innerHTML='<h2 id="project-type-heading">Create a project</h2><p>Choose what you want to work on.</p><div class="project-type-options"><button data-type="cue"><strong>Cue sheet</strong><span>Create a cue sheet with music timings and credits.</span></button><button data-type="reel"><strong>Reels</strong><span>Create a shareable audio playlist with an interactive waveform.</span></button></div><div class="button-row"><button data-cancel>Cancel</button></div>';let type=null;dialog.querySelectorAll('[data-type]').forEach(button=>button.onclick=()=>{type=button.dataset.type;dialog.close();});dialog.querySelector('[data-cancel]').onclick=()=>dialog.close();dialog.onclose=()=>{dialog.remove();resolve(type);};document.body.append(dialog);dialog.showModal();
});}
